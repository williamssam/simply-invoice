# React Native with PNPM

Bootstrapped React Native project with Typescript with pnpm.

To Install

- Install <code>pnpm</code> with <code>npm install -g pnpm</code>
- Run <code>pnpm install</code> to install all dependencies
